# 日常法语 2000 句 · 手机优化版（v3）

本版基于原始单文件 PWA 做了以下调整：

1. **手机顶部更紧凑**：控制区保持双列布局，缩小间距、按钮、输入框和学习/搜索卡片；手机端隐藏不必要的顶部停止键与说明文字。
2. **底部播放条重做**：上一句 / 停止 / 下一句按钮缩小；“单句重复次数”和“循环音色”移到手机底部播放条，可随时切换。
3. **离线词典补全**：页面 2000 句中出现的 640 个规范词条全部有离线条目，不再出现“未收录/暂无释义”的占位；常见动词变位增加原形映射。点击词或原形都会显示 IPA、词性、中文释义并循环朗读。
4. **内置 3 组离线法语音色**：`audio/v1`、`audio/v2`、`audio/v3` 共包含 2000 句及 669 个可点击词形/原形的 MP3，手机端不依赖系统只提供一个 `fr-FR` 音色。内置音频约 58 MB。
5. **循环交替音色**：选择“内置 3 音色交替”后，单句循环、区间每句重复、单词循环都会按 A → B → C 逐轮切换；也保留“系统可用音色交替”。
6. **Service Worker 修正**：本地音频会在使用后进入运行时缓存；离线缺失音频时不再错误地把 `index.html` 当作音频返回。

## 关于内置音色

内置 A/B/C 是**离线法语语音合成音色**（中性 / 女声变体 / 男声变体），不是 Apple/iOS 的系统真人音色，也不是云端神经网络 TTS。优点是无需联网、每台手机表现一致，并且可以真正做到多音色轮换。

如果以后要换成更自然的神经网络法语音色，只要用同名 MP3 覆盖 `audio/v1~v3` 中的文件即可，页面逻辑不需要改。
